# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/pfe

# Optional: override db name for import
IMPORT_DB=plateformeDB

# Optional: clear collections before import
IMPORT_DROP=false

# Server port
PORT=4000

# Auth
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=2h

# Frontend API base URL (Vite)
VITE_API_URL=http://localhost:4000

# Plant recognition APIs
PLANT_ID_API_URL=https://plant.id/api/v3
PLANT_ID_API_KEY=replace-with-plant-id-key
